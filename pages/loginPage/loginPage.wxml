<view class="container">
  <mp-toptips msg="{{errorMsg}}" type="{{error}}" show="{{showError}}"></mp-toptips>
  <view class="xcx-image">
    <image src="../../assets/banner/logo-bg.png"></image>
  </view>
  <view class='title'>瘦西湖票务</view>
  <view class='titleText'>申请获取以下权限</view>
  <view class='contentText'>获得你的公开信息(昵称,头像,手机号等)</view>
  <!-- <view class="authBtn" bindtap='bindGetUserInfo'> 授权用户信息 </view> -->
  <button class="btn" open-type="getUserInfo" bindgetuserinfo="bindGetUserInfo" type="primary" > 授权用户信息 </button>
</view>
